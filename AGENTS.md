# AGENTS.md — 个人知识仓库整理规范

这是一个个人学习知识仓库。本文件约束所有进入本项目的 agent 的行为。

## 仓库结构

```
notes/
├── AGENTS.md            # 本文件
├── raw/                 # 未分类原材料：用户不确定归属时先放这里，由 agent 判断归类
└── <技术分类>/           # 如 Git、Nginx、数据库、......
    ├── raw/             # 原材料：未整理的文本笔记、截图等
    └── <主题>.md        # 整理后的笔记（可按主题建子文件夹，如 数据库/MySQL/）
```

## Agent 的任务

用户会手动把原材料放进某个分类目录的 `raw/` 文件夹；不确定归属时，也会放进根目录的 `raw/` 文件夹。agent 负责将其整理成规范的 Markdown 笔记。

### 工作流程

1. **扫描**：检查根目录的 `raw/` 文件夹和各分类目录下的 `raw/` 文件夹，找出尚未整理的原材料。
2. **判断归属**：
   - 原材料属于某篇已有笔记的主题 → 将内容整理、合并进该笔记（去重、补充、整合）。不一定只是追加，可以调整笔记结构。
   - 是全新主题 → 新建笔记文件。
   - 归属判断的同时，按下方「内容取舍原则」筛选值得记录的内容；不值得记的可省略，并在汇报中说明省略了什么。
3. **确定输出位置**：
   - 对应分类目录下已存在该主题的子文件夹（如 `数据库/MySQL/`）→ 放入该文件夹，文件名与主题一致（如 `MySQL.md`）。
   - 否则直接放在分类目录下（如 `Git/Git多人协作指南.md`）。
   - 根目录 `raw/` 中的原材料 → 由 agent 根据内容判断归属：
     - 能归入现有分类 → 按上面两条规则放入对应位置。
     - 没有合适的现有分类 → **新建分类目录**，新建前需要询问用户是否同意。(根目录下也是可以新建分类的)
   - **不要**把整理结果写进 `raw/`。
4. **不要删除raw/目录下的文件**
5. **汇报**：整理结束后向用户报告：新增/修改了哪些文件、原材料覆盖了哪些知识点、是否有无法识别的内容（如截图模糊、内容不完整）需要用户确认。

## 内容取舍原则

AI工具时代，语法和API细节随时可查，笔记的价值在于**难以快速重建的理解**。整理任何内容时，站在**架构师 / 产品经理视角**判断：这个知识解决什么问题、为什么这样设计、什么时候该用，而不是逐行关心代码怎么实现。

**值得记录**：
- **原理与机制**：为什么会这样、底层如何运作（如索引结构、事件模型、缓存失效策略）。
- **设计权衡与选型依据**：为什么用A不用B、各自的适用场景与局限。
- **架构视角的框架理解**：框架解决什么问题、核心抽象是什么、在整个技术栈中的位置。
- **踩坑经验与排错思路**
- **使用AI的心得、经验**

**不值得记录**：
- 纯语法、API参数列表、可直接查官方文档的参考性内容。
- 某一个方法/函数在代码中具体的实现。（简要展示代码或总结代码框架即可）
> 对于此类知识点，说明一下。比如："（在你遇到...问题时，/在你需要...时）询问AI...即可"。  
> 该原则不要求重写已有笔记，除非用户明确指定。

在初步整理了 raw/ 目录下的笔记后，用户后续让你"解释一下X, 补进笔记"，在对话框解释时可以详细说明，但写进笔记时要确保过一遍上面的内容取舍原则，交付值得留下的理解。

## 笔记风格规范

参考仓库内已有笔记的写法，遵循以下约定：

- 全文使用**中文**。
- 风格是**重点式笔记**，不是长篇教程：条目化、短句、直接给结论。
- 关键术语和重要结论用 `**加粗**`，而对于“概念”、“场景”、“注意”这类标题性名词不要加粗。
- 代码块必须标注语言（bash、sql、nginx 等），命令后可跟简短注释说明作用。
- 补充说明、注意事项用 `> 引用块`。
- 笔记中的 HTML 注释 `<!-- -->` 是用户自己标记"尚未理解的内容"用的，agent **不要**添加 HTML 注释；合并进已有笔记时**保留**其中的已有注释，不要改动或删除。
- 灵活运用表格来对类似的项进行对比
- 内容适合时用**Mermaid**辅助表达：流程、架构与模块关系、时序、状态流转等用图比文字更清晰的场景。
  - 图要**简洁**，只保留关键节点和主干逻辑，不堆砌细节。
  - 简单列表或一句话能说清的内容**不要**强行画图。

## 其他约束

- 只处理笔记整理相关的工作，不要改动与本任务无关的文件。
- 对原材料内容有疑问时（如截图看不清、语义不完整），先询问用户，不要自行编造。
- 对原材料进行整理时，如果发现有误或者有什么知识点需要补充时，不要擅自修改或增加内容；在完成整理后，在汇报阶段向用户询问是否需要修改即可。

